package edaclikt

import com.github.ajalt.clikt.core.CliktCommand

/**
 * Represents the main entry point for the CLI application.
 * to which all commands are added
 */
class CLI: CliktCommand() {
    override fun run() = Unit
}
